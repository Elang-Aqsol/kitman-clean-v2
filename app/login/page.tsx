import CardLogin from "@/components/custom-components/loginForm";

export default function LoginPage() {
  return (
    <main className="flex flex-col items-center justify-center md:h-screen md:w-screen">
      <CardLogin />
    </main>
  );
}
